import { Navbar } from "./navBar";
import "../styles/Header.css"; // Import the CSS file

export const Header = () => {
  return (
    <header className="header">
      <Navbar />
    </header>
  );
};
